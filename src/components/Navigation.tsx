import React from "react";

const Navigation = () => {
    return(
        <div>
            <a>Feed</a>
            <a>Favorites</a>
            <a>Account</a>
            <a>Logout</a>
        </div>
    );
}

export default Navigation;